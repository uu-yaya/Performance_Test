# 桌宠性能采集操作指南：从环境准备开始

这份指南用于边测试边复制命令。BongoCat 当前按你的实际情况使用进程名 `bongo-cat`。

## 0. 测试前默认信息

```powershell
$ProjectDir = "D:\desktop_pet"
$ExcelPath = "D:\desktop_pet\桌宠类产品性能测试记录表.xlsx"
$Collector = "D:\desktop_pet\tools\collect_desktop_pet_perf.ps1"
$Importer = "D:\desktop_pet\tools\import_perf_results.py"
$Python = "C:\Users\zoeuuliu\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe"
```

BongoCat 默认：

```powershell
$ProductId = "P001"
$ProductName = "BongoCat"
$ProcessName = "bongo-cat"
```

如果你不确定真实进程名，先启动产品，再运行：

```powershell
Get-Process | Sort-Object ProcessName | Select-Object ProcessName,Id,CPU,WorkingSet64 | Out-GridView
```

如果 BongoCat 没启动，可以尝试：

```powershell
Start-Process "C:\Program Files\BongoCat\bongo-cat.exe"
```

正式采集前一定先确认进程能被找到：

```powershell
Get-Process -Name bongo-cat -ErrorAction SilentlyContinue |
  Select-Object ProcessName,Id,CPU,@{Name="MemoryMB";Expression={[math]::Round($_.WorkingSet64/1MB,1)}} |
  Format-Table -AutoSize
```

如果没有输出，不要开始采集；先确认 BongoCat 是否已经启动，以及进程名是否正确。

---

## 1. 重启电脑并等待

手动重启电脑，重启后等待 5 分钟，让系统后台任务稳定。

记录当前时间：

```powershell
Get-Date
```

---

## 2. 关闭干扰应用

建议关闭浏览器、微信/QQ/Discord、Steam 下载、云盘同步、游戏启动器、录屏/直播、IDE、其他桌宠或壁纸软件。

查看 CPU 高占用进程：

```powershell
Get-Process |
  Sort-Object CPU -Descending |
  Select-Object -First 20 ProcessName,Id,CPU,@{Name="MemoryMB";Expression={[math]::Round($_.WorkingSet64/1MB,1)}} |
  Format-Table -AutoSize
```

查看内存高占用进程：

```powershell
Get-Process |
  Sort-Object WorkingSet64 -Descending |
  Select-Object -First 20 ProcessName,Id,@{Name="MemoryMB";Expression={[math]::Round($_.WorkingSet64/1MB,1)}} |
  Format-Table -AutoSize
```

---

## 3. 记录测试环境

系统版本：

```powershell
Get-ComputerInfo |
  Select-Object WindowsProductName,WindowsVersion,OsHardwareAbstractionLayer,OsTotalVisibleMemorySize |
  Format-List
```

CPU：

```powershell
Get-CimInstance Win32_Processor |
  Select-Object Name,NumberOfCores,NumberOfLogicalProcessors,MaxClockSpeed |
  Format-List
```

GPU：

```powershell
Get-CimInstance Win32_VideoController |
  Select-Object Name,AdapterRAM,DriverVersion,VideoModeDescription |
  Format-List
```

分辨率和刷新率：

```powershell
Get-CimInstance Win32_VideoController |
  Select-Object Name,CurrentHorizontalResolution,CurrentVerticalResolution,CurrentRefreshRate |
  Format-Table -AutoSize
```

电源模式：

```powershell
powercfg /GETACTIVESCHEME
```

---

## 4. 创建测试输出目录

```powershell
New-Item -ItemType Directory -Path D:\desktop_pet\perf_runs -Force
New-Item -ItemType Directory -Path D:\desktop_pet\external_logs -Force
```

---

## 5. 采集系统基线

不要启动 BongoCat，先采 5 分钟桌面基线：

```powershell
powershell -ExecutionPolicy Bypass -File D:\desktop_pet\tools\collect_desktop_pet_perf.ps1 `
  -ProductId BASELINE `
  -ProductName SystemBaseline `
  -ProcessName explorer `
  -Scenario 系统基线 `
  -DurationSec 300 `
  -IntervalSec 1
```

---

## 6. 启动并确认 BongoCat

启动 BongoCat 后等待 30-60 秒，再确认进程：

```powershell
Get-Process -Name bongo-cat -ErrorAction SilentlyContinue |
  Select-Object ProcessName,Id,CPU,@{Name="MemoryMB";Expression={[math]::Round($_.WorkingSet64/1MB,1)}} |
  Format-Table -AutoSize
```

如果没有输出，说明进程名没匹配上，采集结果会全是 0。

---

## 7. 采集启动峰值

先关闭 BongoCat，运行命令后立刻启动 BongoCat，采 2 分钟：

```powershell
powershell -ExecutionPolicy Bypass -File D:\desktop_pet\tools\collect_desktop_pet_perf.ps1 `
  -ProductId P001 `
  -ProductName BongoCat `
  -ProcessName bongo-cat `
  -Scenario 启动峰值 `
  -DurationSec 120 `
  -IntervalSec 1
```

---

## 8. 采集空闲常驻

BongoCat 已启动并稳定后，不要操作，采 10 分钟：

```powershell
powershell -ExecutionPolicy Bypass -File D:\desktop_pet\tools\collect_desktop_pet_perf.ps1 `
  -ProductId P001 `
  -ProductName BongoCat `
  -ProcessName bongo-cat `
  -Scenario 空闲常驻 `
  -DurationSec 600 `
  -IntervalSec 1
```

---

## 9. 采集基础交互

运行命令后，在 5 分钟内点击、拖拽、移动、隐藏/显示 BongoCat：

```powershell
powershell -ExecutionPolicy Bypass -File D:\desktop_pet\tools\collect_desktop_pet_perf.ps1 `
  -ProductId P001 `
  -ProductName BongoCat `
  -ProcessName bongo-cat `
  -Scenario 基础交互 `
  -DurationSec 300 `
  -IntervalSec 1
```

---

## 10. 采集长时间稳定

标准 1 小时：

```powershell
powershell -ExecutionPolicy Bypass -File D:\desktop_pet\tools\collect_desktop_pet_perf.ps1 `
  -ProductId P001 `
  -ProductName BongoCat `
  -ProcessName bongo-cat `
  -Scenario 长时间稳定 `
  -DurationSec 3600 `
  -IntervalSec 1
```

更严格的 4 小时：

```powershell
powershell -ExecutionPolicy Bypass -File D:\desktop_pet\tools\collect_desktop_pet_perf.ps1 `
  -ProductId P001 `
  -ProductName BongoCat `
  -ProcessName bongo-cat `
  -Scenario 长时间稳定4小时 `
  -DurationSec 14400 `
  -IntervalSec 1
```

---

## 11. 游戏影响测试

FPS、1% Low、帧时间需要 PresentMon 或 NVIDIA FrameView 额外导出 CSV。

游戏 + BongoCat 时，同时运行：

```powershell
powershell -ExecutionPolicy Bypass -File D:\desktop_pet\tools\collect_desktop_pet_perf.ps1 `
  -ProductId P001 `
  -ProductName BongoCat `
  -ProcessName bongo-cat `
  -Scenario 游戏影响 `
  -DurationSec 600 `
  -IntervalSec 1
```

---

## 12. 温度和功耗

温度、功耗建议用 HWiNFO Sensors Logging 导出 CSV，保存到：

```text
D:\desktop_pet\external_logs\hwinfo_bongocat.csv
```

---

## 13. 导入 Excel

普通导入：

```powershell
& C:\Users\zoeuuliu\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe `
  D:\desktop_pet\tools\import_perf_results.py
```

如果有 PresentMon/FrameView 和 HWiNFO 日志：

```powershell
& C:\Users\zoeuuliu\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe `
  D:\desktop_pet\tools\import_perf_results.py `
  --presentmon-csv D:\desktop_pet\external_logs\presentmon_game_with_pet.csv `
  --hwinfo-csv D:\desktop_pet\external_logs\hwinfo_bongocat.csv
```

打开 Excel：

```powershell
Start-Process "D:\desktop_pet\桌宠类产品性能测试记录表.xlsx"
```

重点看 `场景记录`、`采样数据`、`汇总评分`、`自动采集导入`。

---

## 14. 关闭后确认残留

关闭 BongoCat 后运行：

```powershell
Get-Process -Name bongo-cat -ErrorAction SilentlyContinue
```

如果仍然有输出，在 Excel 的 `问题记录` 中记录为残留进程或无法完全退出。

## 最终测试产品清单

| 产品ID | 产品名称 | 来源/AppID | 启动/路径 | 主进程名 | 子进程/依赖 | 备注 |
|---|---|---|---|---|---|---|
| P001 | BongoCat | GitHub Release/本地安装 / v1.1.0 | exe 直接运行<br>C:\Program Files\BongoCat\bongo-cat.exe<br>安装包：D:\desktop_pet\BongoCat\BongoCat_1.1.0_x64-setup.exe | bongo-cat | msedgewebview2 | 实际测试来源为 GitHub 下载的安装包；运行时确认：bongo-cat.exe；子进程 msedgewebview2.exe |
| P002 | Desktop Mate | Steam / 3301060 | steam://rungameid/3301060<br>C:\Program Files (x86)\Steam\steamapps\common\Desktop Mate | DesktopMate | 未观察到固定子进程 | 运行时确认：DesktopMate.exe |
| P003 | VPet-Simulator | Steam / 1920960 | steam://rungameid/1920960<br>C:\Program Files (x86)\Steam\steamapps\common\VPet | VPet-Simulator.Windows | 未观察到固定子进程 | 运行时确认：VPet-Simulator.Windows.exe |
| P004 | Desktop Goose | itch.io / v0.31 | itch.io 安装包 / exe 直接运行<br>C:\Users\zoeuuliu\Downloads\Desktop Goose v0.31\DesktopGoose v0.31\GooseDesktop.exe<br>来源页：https://samperson.itch.io/desktop-goose<br>下载包：C:\Users\zoeuuliu\Downloads\Desktop Goose v0.31.zip | GooseDesktop | 未观察到固定子进程 | 运行时确认：GooseDesktop.exe |
| P005 | Ai Vpet | Steam / 3029820 | steam://rungameid/3029820<br>C:\Program Files (x86)\Steam\steamapps\common\Ai Vpet | Ai Vpet | UnityCrashHandler64 | 运行时确认：Ai Vpet.exe；子进程 UnityCrashHandler64.exe |
| P006 | Tiny Pasture | Steam / 3167550 | steam://rungameid/3167550<br>C:\Program Files (x86)\Steam\steamapps\common\TinyPasture | TinyPasture | 未观察到固定子进程 | 运行时确认：TinyPasture.exe |
| P007 | Pocket Waifu:Desktop Pet | Steam / 2989820 | steam://rungameid/2989820<br>C:\Program Files (x86)\Steam\steamapps\common\Pocket Waifu Pet | PocketWaifu | UnityCrashHandler64 | 运行时确认：PocketWaifu.exe；子进程 UnityCrashHandler64.exe |
| P008 | Rusty's Retirement | Steam / 2666510 | steam://rungameid/2666510<br>C:\Program Files (x86)\Steam\steamapps\common\Rusty's Retirement | Rusty's Retirement | UnityCrashHandler64 | 运行时确认：Rusty's Retirement.exe；子进程 UnityCrashHandler64.exe |
| P009 | MateEngine | Steam / 3625270 | steam://rungameid/3625270<br>C:\Program Files (x86)\Steam\steamapps\common\MateEngine | MateEngineX | UnityCrashHandler64 | 运行时确认：MateEngineX.exe；子进程 UnityCrashHandler64.exe |
| P010 | Ai桌面宠物 | Steam / 4227700 | steam://rungameid/4227700<br>C:\Program Files (x86)\Steam\steamapps\common\AIDesktopPet | AIDesktopPet | AIDesktopPet 多进程；AI 功能可能拉起 python/pythonw/ffmpeg/node | 运行时确认：AIDesktopPet.exe 多进程 |
| P011 | 角落里的艾果 Eggo | Steam / 3700760 | steam://rungameid/3700760<br>C:\Program Files (x86)\Steam\steamapps\common\Eggo | Eggo | 未观察到固定子进程 | 运行时确认：Eggo.exe |
| P012 | Molili Ai Friends: Ai 桌面宠物 | Steam / 4141770 | steam://rungameid/4141770<br>C:\Program Files (x86)\Steam\steamapps\common\Molili | Molili | msedgewebview2 | 运行时确认：Molili.exe；子进程 msedgewebview2.exe |
| P013 | 猫娘计划 - N.E.K.O | Steam / 4099310 | steam://rungameid/4099310<br>C:\Program Files (x86)\Steam\steamapps\common\n.e.k.o | N.E.K.O | N.E.K.O 多进程；projectneko_server/openfang | 运行时确认：N.E.K.O.exe 多进程；子进程 projectneko_server.exe、openfang.exe |

以上主进程名均按运行时确认结果填写；采集命令里的 `-ProcessName` 使用表中的主进程名，不要带 `.exe`。

如果产品开启 AI/语音/模型下载等功能，可能临时出现额外子进程；正式采集时保留默认的“采集主进程+子进程树”逻辑。

---
